<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      BrowserTabActivity is already fully declared by MSAL's own AAR manifest.
      We do NOT redeclare it here — that caused the placeholder and merger conflicts.

      The app's AndroidManifest.xml must declare the BrowserTabActivity intent-filter
      with the app-specific msauth redirect URI. See the plugin README for instructions.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
